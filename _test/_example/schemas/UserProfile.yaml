firstName:
  type: string
  required: true

lastName:
  type: string
  required: true

age:
  type: number

gender:
  $ref: Gender
  default: Male

friends:
  type: array
  default: []
  items:
    type: string
