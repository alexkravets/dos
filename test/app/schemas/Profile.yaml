firstName:
  type: string

lastName:
  type: string

age:
  type: number

sex:
  $ref: ProfileSex

type:
  type: string

parameters:
  $ref: ProfileParameters

alternativeParameters:
  $ref: ProfileParameters

tags:
  type: array
  items:
    $ref: ProfileTag

isActive:
  type:    boolean
  default: false
